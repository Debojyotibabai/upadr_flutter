import 'package:flutter/material.dart';

class AppPrimaryTextField extends StatelessWidget {
  const AppPrimaryTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return TextFormField(
      decoration: InputDecoration(),
    );
  }
}
